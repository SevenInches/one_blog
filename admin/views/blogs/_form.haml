- error = @blog.errors.include?(:title)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :title, :class => 'control-label'
  .controls
    =f.text_field :title, :class => 'form-control input-large input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:title, :class => 'text-error') : pat(:example)
- error = @blog.errors.include?(:abstract)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :abstract, :class => 'control-label'
  .controls
    =f.text_field :abstract, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:abstract, :class => 'text-error') : pat(:example)
- error = @blog.errors.include?(:view)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :view, :class => 'control-label'
  .controls
    =f.text_field :view, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:view, :class => 'text-error') : pat(:example)
- error = @blog.errors.include?(:tag_id)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :tag_id, :class => 'control-label'
  .controls
    =f.select :tag_id, :options =>@tags
    %span.help-inline=error ? f.error_message_on(:tag_id, :class => 'text-error') : pat(:example)    
- error = @blog.errors.include?(:body)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :body, :class => 'control-label'
  .controls
    ~f.text_area :body, :class => 'form-control input-large input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:body, :class => 'text-error') : pat(:example)

.form-actions
  =f.submit pat(:save), :class => 'btn btn-primary'
  &nbsp;
  =f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  &nbsp;
  =link_to pat(:cancel), url(:blogs, :index), :class => 'btn btn-default'